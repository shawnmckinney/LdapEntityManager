binddn=dc=example,dc=com
bindpw=secret
host=localhost
port=389
# max connections:
max=10
# TLS and LDAPS must not be used simulataneously:
tls=false
ldaps=false
trust.store=truststore.jks
trust.store.pw=changeit
trust.store.classpath=true